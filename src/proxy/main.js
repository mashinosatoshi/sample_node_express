import user from "./user"
/**
 * 判らないことがあれば、誰かに何かに聞かなければならないのはどこの世界でも同じだな
 */

export default function () { 
    console.log(user.execute1());
    console.log(user.execute2());
    console.log(user.execute3());
}